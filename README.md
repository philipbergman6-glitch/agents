# agents

Agents built on Claude Code.

First agent: a Warren Buffett–style company-diagnosis agent — deterministic scoring engine backed by real financial data, with Claude narrating the diagnosis.

Planning happens on the [wayfinder map](../../issues) (issue labelled `wayfinder:map`).
